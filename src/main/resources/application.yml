spring:
  application:
    name: eStore

  datasource:
    url: jdbc:mysql://localhost:3306/mydb
    username: root
    password: 123456

  jpa:
    hibernate:
      ddl-auto: update

  mail:
    host: smtp.gmail.com
    port: 587
    username: bb10102004@gmail.com
    password: izcv rlfq esaw wcwd
    default-encoding: UTF-8
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

server:
  port: 9091
  address: 0.0.0.0

contact:
  support-email: bb10102004@gmail.com

openai:
  api-key: 
  model: ${OPENAI_MODEL:gpt-5.4-mini}
  responses-url: ${OPENAI_RESPONSES_URL:https://api.openai.com/v1/responses}
  timeout-seconds: ${OPENAI_TIMEOUT_SECONDS:30}
  max-output-tokens: ${OPENAI_MAX_OUTPUT_TOKENS:700}
  max-catalog-products: ${OPENAI_MAX_CATALOG_PRODUCTS:40}

vnpay:
  tmn-code: ${VNPAY_TMN_CODE:BUIGSP00}
  hash-secret: ${VNPAY_HASH_SECRET:18BL3SIPGILL9JY469XOMV4W84K6WEQ6}
  pay-url: ${VNPAY_PAY_URL:https://sandbox.vnpayment.vn/paymentv2/vpcpay.html}
  return-url: ${VNPAY_RETURN_URL:http://localhost:3000/payment/vnpay-return}
  ipn-url: ${VNPAY_IPN_URL:http://localhost:9090/api/payments/vnpay/ipn}
  version: ${VNPAY_VERSION:2.1.0}
  command: ${VNPAY_COMMAND:pay}
  currency-code: ${VNPAY_CURRENCY_CODE:VND}
  locale: ${VNPAY_LOCALE:vn}
  order-type: ${VNPAY_ORDER_TYPE:other}
#    show-sql: true
#logging:
#    level: DEBUG
##    file: eStore.log
##    pattern: "%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n"
